# Backlog

- [ ] Deploy DNS server (bind9/unbound) on separate macvlan IP (192.168.1.6)
- [ ] Deploy reverse proxy (Caddy/Traefik) for multi-service routing
- [ ] Add healthcheck to compose file
- [ ] Add logging configuration (json-file max-size/max-file)
- [ ] Support environment variable overrides for container config
